import "@fontsource/lato";

const fontsConfig = {
  heading: `'Lato', sans-serif`,
  body: `'Lato', sans-serif`,
};

const fontSizeConfig = {
  sm: "12px",
  md: "14px",
  lg: "16px",
};

export { fontsConfig, fontSizeConfig };
